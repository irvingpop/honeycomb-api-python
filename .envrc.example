# .envrc.example
# Copy to .envrc and fill in your values
# .envrc is gitignored - never commit it
#
# After creating .envrc, run: direnv allow

# =============================================================================
# MANAGEMENT KEY (for creating test environments and API keys)
# =============================================================================
# Get from: Honeycomb UI > Team Settings > Management Keys
# Required for: live-tester agent, v2 API resources (environments, API keys)

export HONEYCOMB_MANAGEMENT_KEY="hcamk_01xxxxxxxxxxxx"
export HONEYCOMB_MANAGEMENT_SECRET="xxxxxxxxxxxxxxxx"

# =============================================================================
# TEST SESSION (auto-generated by live-tester agent)
# =============================================================================
# The live-tester agent creates test environments and API keys automatically.
# It stores the generated credentials in .claude/secrets/test.env
# This block loads them if they exist.

if [ -f .claude/secrets/test.env ]; then
    source .claude/secrets/test.env
    echo "Loaded test session: ${HONEYCOMB_TEST_DATASET:-unknown}"
fi

# =============================================================================
# DIRECT API KEY (alternative to management key workflow)
# =============================================================================
# If you prefer to use an existing API key instead of creating test environments,
# uncomment and set these. The live-tester agent will use this directly.

# export HONEYCOMB_API_KEY="hcaik_01xxxxxxxxxxxx"
# export HONEYCOMB_TEST_DATASET="your-existing-dataset"
